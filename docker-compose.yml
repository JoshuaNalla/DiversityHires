version: "3.8"
services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    volumes:
      # Optional: bind mount code so we can hot reload main.py
      - ./backend:/app
    environment:
      - PYTHONUNBUFFERED=1
